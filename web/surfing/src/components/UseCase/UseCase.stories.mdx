import { Meta, Title, Subtitle, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import UseCase from "./";

export const useCaseData = {
  viewportWidth: 500,
  useCase: {
    img: "https://fluidity.money/assets/images/useCaseIcons/forReceivers.png",
    title: "For senders",
    info: "Every time you use fluid assets in any use case, you earn yield. Make purchases and send money as you normally would, but with the added potential to win rewards.",
  }
}

<Meta title={"Container/UseCase"}/>

<Title>Use Case</Title>
<Subtitle>
  Highlighting the different use cases for Fluidity.
</Subtitle>

export const Template = (args) => <UseCase {...args} />

<Canvas>
  <Story name="Use Case" args={useCaseData}>
    {Template.bind({})}
  </Story>
</Canvas>