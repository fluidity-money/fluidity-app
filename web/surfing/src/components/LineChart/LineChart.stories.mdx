import { Meta, Canvas, Story, Title, Subtitle, ArgsTable } from "@storybook/addon-docs";

import LineChart from "./LineChart";

<Meta title="Data/Line Chart" component={LineChart} />

export const Template = (args) => <div style={{height: "400px"}}>
    <LineChart {...args} />
</div>;

<Title>Line Chart</Title>
<Subtitle>A beautiful scalable line chart designed for living data</Subtitle>

<ArgsTable of={LineChart} />

<Canvas withToolbar>
    <Story
        name="Line Graph"
        args={{
        data: [{x: 10, y: 10, name: "Alice"}, {x: 20, y: 20, name: "Bob"}, {x: 30, y: 30, name: "Connor"}, {x: 40, y: 20, name: "David"}],
        xLabel: 'Some X Label',
        yLabel: 'Some Y Label',
        lineLabel: 'Some Line Label',
        accessors: {
            xAccessor: (d) => d.x,
            yAccessor: (d) => d.y,
        },
        renderTooltip: ({datum}) => (
            <span>
              {datum.y}{" "}
              <span style={{ color: "rgba(255,255,255, 50%)" }}>
                By {datum.name}
              </span>
            </span>
        )
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="0d Line Graph"
        args={{
        data: [{x: 10, y: 0, name: "Alice"}, {x: 20, y: 0, name: "Bob"}, {x: 30, y: 0, name: "Connor"}, {x: 40, y: 0, name: "David"}],
        xLabel: 'Some X Label',
        yLabel: 'Some Y Label',
        lineLabel: 'Some Line Label',
        accessors: {
            xAccessor: (d) => d.x,
            yAccessor: (d) => d.y,
        },
        renderTooltip: ({datum}) => (
            <span>
              {datum.y}{" "}
              <span style={{ color: "rgba(255,255,255, 50%)" }}>
                By {datum.name}
              </span>
            </span>
        )
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
