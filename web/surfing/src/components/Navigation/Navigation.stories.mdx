import { Canvas, Meta, Story } from "@storybook/addon-docs";

import Navigation from "./Navigation";

<Meta title="Navigation/ecosystem" component={Navigation} />

export const Template = (args) => <Navigation {...args} />;

# Navigation

<Canvas>
  <Story
    name="ecosystem"
    args={{
      page: "ecosystem",
      pageLocations: ["all projects", "featured projects"],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
