import { Meta, Canvas, Story, Title, Subtitle } from "@storybook/addon-docs";

import HoverButton from "./HoverButton";

<Meta
    title="Button/Hover Button"
    component={HoverButton}
    parameters={{
        pseudo: {
            hover: ".hover",
            focus: ".focus",
        }
    }}
/>

export const Template = ({children, ...props}) => <HoverButton {...props}>
    {children}
</HoverButton>;

<Canvas columns={1}  withSource="open" withToolbar >
    <Title>Link Button</Title>
    <Subtitle>Button to toggle filters</Subtitle>
    
    <h2>Link Button</h2>
    <Story
        name="Link Button Large"
        args={{
            children: "Label",
            size: "large",
            hoverComp: "hello",
        }}
    >
        {Template.bind({})}
    </Story>
    <h2>Link Button</h2>
    <Story
        name="Link Button Medium"
        args={{
            children: "Label",
            size: "medium",
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Link Button Small"
        args={{
            children: "Label",
            size: "small",
        }}
    >
        {Template.bind({})}
    </Story>
    
    <h2>HoverButton States</h2>
    <Story
        name="Link Button Hover"
        args={{
            className: "hover",
            children: "Label",
            size: "large",
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Link Button Focus"
        args={{
            className: "focus",
            children: "Label",
            size: "large",
        }}
    >
        {Template.bind({})}
    </Story>
    
    <h2>Disabled</h2>
    <Story
        name="Link Button Disabled"
        args={{
            children: "Label",
            size: "large",
            disabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
