import { Meta, Canvas, Story, Title, Subtitle } from "@storybook/addon-docs";

import ChainSelectorButton from "./ChainSelectorButton";
import { ReactComponent as ReactSvg } from "/src/assets/react.svg";

<Meta 
    title="Button/ChainSelector Button"
    component={ChainSelectorButton}
    parameters={{
        pseudo: {
            hover: ".hover",
            focus: ".focus",
        }
    }}
/>

export const Template = (props) => <ChainSelectorButton {...props} />


<Canvas columns={1}  withSource="open" withToolbar >
    <Title>ChainSelector Button</Title>
    <Subtitle>Button to switch Chains</Subtitle>
    
    <h2>Default</h2>
    <Story
        name="ChainSelector Button Default"
        args={{
            chain: {
                name: "OPT",
                icon: <ReactSvg />
            },
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
    
    <h2>Button Hover</h2>
    <Story
        name="ChainSelector Button Hover"
        args={{
            className: "hover",
            chain: {
                name: "OPT",
                icon: <ReactSvg />
            },
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
    
    <h2>Button Focus</h2>
    <Story
        name="ChainSelector Button Focus"
        args={{
            className: "focus",
            chain: {
                name: "OPT",
                icon: <ReactSvg />
            },
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
    
    <h2>Button Disabled</h2>
    <Story
        name="ChainSelector Button Disabled"
        args={{
            disabled: true,
            chain: {
                name: "OPT",
                icon: <ReactSvg />
            },
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
