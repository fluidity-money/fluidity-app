import { Meta, Canvas, Story, Title, Subtitle } from "@storybook/addon-docs";

import { SliderButton } from "./";
import Text from "../../Text";

<Meta
    title="Button/SliderButton"
    component={SliderButton}
/>

export const Template = (props) => <SliderButton {...props} />

<Canvas columns={1} withSource="open" withToolbar >
    <Title>Slider Button</Title>
    <Subtitle>For confirming extra special things</Subtitle>
    <Story
        name="Slider Button"
        args={{
            children: <Text prominent>SLIDE TO STAKE</Text>,
            onSlideComplete: () => { return },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>