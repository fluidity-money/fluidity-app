import {
  Meta,
  Title,
  Subtitle,
  Story,
  Canvas,
  ArgsTable,
} from "@storybook/addon-docs";
import LoadingDots from "./LoadingDots";

<Meta title={"Loading/LoadingDots"} component={LoadingDots} />

<Title>Loading Dots</Title>

<Subtitle>Loading dots</Subtitle>

export const Template = (args) => <LoadingDots />;

<Canvas>
  <Story name="LoadingDots" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
