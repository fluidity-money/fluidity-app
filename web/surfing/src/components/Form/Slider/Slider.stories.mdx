import { Meta, Canvas, Story, Title, Subtitle } from "@storybook/addon-docs";

import { Slider } from "./";

<Meta
    title="Form/Slider"
    component={Slider}
/>

export const Template = (props) => <div style={{
  maxWidth: 200,
  marginLeft: 800
}}>
  <Slider {...props} />
</div>

<Canvas columns={1} withSource="open" withToolbar >
    <Title>Form</Title>
    <Subtitle>For forming things</Subtitle>
    <Story
        name="Slider"
        args={{
          min: 31,
          max: 365,
          step: 1,
          valueCallback: (value) => {
            console.log(value);
          }
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>