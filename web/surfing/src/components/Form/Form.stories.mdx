import { Meta, Canvas, Story, Title, Subtitle, ArgsTable } from "@storybook/addon-docs";

import Form from "./";
import { Card } from '../';

<Meta title="Form" component={Form.Toggle} />

export const Template = (args) => (
  <Card
    color="holo"
  >
    <Form.Toggle {...args}/>
  </Card>
);

<Title>Form</Title>

<Canvas>
    <Story
        name="Checkbox (Toggle)"
        args={{
            label: "Checkbox",
            color: 'black',
            disabled: false,
            direction: "horizontal",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
