import { Meta, Canvas, Story, Title, Subtitle, ArgsTable } from "@storybook/addon-docs";

import { Toggle } from "./";
import { Card } from '../';

<Meta title="Form" component={Toggle} />

export const Template = (args) => (
  <Card
    color="holo"
  >
    <Toggle {...args}/>
  </Card>
);

<Title>Form</Title>

<Canvas>
    <Story
        name="Checkbox (Toggle)"
        args={{
            label: "Checkbox",
            color: 'black',
            disabled: false,
            direction: "horizontal",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
